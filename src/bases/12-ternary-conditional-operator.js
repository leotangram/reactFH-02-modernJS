// Ternary conditional operator

const active = true

// let message = ''

// if (!active) message = 'Activo'
// else message = 'Inactivo'

// const message = active ? 'Activo' : 'Inactivo'
const message = active && 'Activo'

console.log(message)
